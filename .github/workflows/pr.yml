name: dbt-project-checks

on:
  pull_request:
    branches:
      - develop

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
      # - id: file_changes
      #   uses: trilom/file-changes-action@v1.2.4
      #   with:
      #     output: ' '
      - uses: dbt-checkpoint/dbt-checkpoint@v1.1.1
        env:
          DBT_ACCESS_TOKEN: ${{ secrets.DBT_ACCESS_TOKEN }}
      #  with:
      #    args: run --files ${{ steps.file_changes.outputs.files}}
